import styled from 'styled-components';

const PageHeaderWrap = styled.div`
    .ant-layout-header {
        display: flex;
        align-items: center;
        padding: 0;

        .ant-breadcrumb-link {
            font-size: 14px;
        }
    }
`;

export default PageHeaderWrap;
